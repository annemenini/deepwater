const baseURL = 'https://storage.googleapis.com/deepwater/model';
const version = 'v5';

export const modelURL = `${baseURL}/${version}/tensorflowjs_model.pb`
export const weightsURL = `${baseURL}/${version}/weights_manifest.json`